# Core
torch>=2.0.0
# transformers must know Qwen3.6 (released Apr 2026). 5.6.0 is the floor;
# exact working pin gets verified on the pod in pod_setup_grpo.sh. Model
# loading uses trust_remote_code=True so HF can fetch the modeling code
# directly if the installed transformers lags the model release.
transformers>=5.6.0
accelerate>=0.27.0

# Vision models
timm>=0.9.0
einops>=0.7.0

# Claude API (baseline comparisons)
anthropic>=0.25.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.9.0

# Data handling
pyyaml>=6.0
pandas>=2.0.0
polars>=0.19.0
pyarrow>=10.0.0
awpy>=2.0.0

# CS2 demo tools (parsing, viewer export, screenshot capture)
cs2-tools[parse] @ git+https://github.com/skkwowee/cs2-tools.git

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
datasets>=2.14.0

# Training
trl>=0.12.0
peft>=0.13.0
bitsandbytes>=0.43.0
faiss-cpu>=1.7.0  # RECALL kNN advantage estimation in GRPO trainer

# Optional: Flash attention for faster inference
# pip install flash-attn --no-build-isolation
