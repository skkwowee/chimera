# Chimera — Session Progress

# >>> WHY decisions were made (killed/kept/eval-traps/the Line in the Sand):
# >>> see docs/decisions-ledger.md. THIS file tracks run STATE; that file
# >>> tracks WHY. Add killed/refined ideas there, not here.

# ============================================================================
# 2026-07-18: METHODOLOGY RESET v2 — adversarial review + locked recipe.
# READ THIS SECTION FIRST. The ORDERED RUNBOOK below is the source of truth for
# what to do next in ANY session. Do steps IN ORDER; each has a done-check.
# ============================================================================
#
# Context: two ultracode workflows ran 2026-07-18:
#   docs/adversarial-review.md      — 24 confirmed findings (4 critical); R1 was
#                                     blocked; criticals+majors FIXED same day
#                                     (commits 3c3d8d0, ef349ac).
#   docs/retrain-recipe.md          — Knobs 1-7 ALL LOCKED (details for 4-7 in
#     + retrain-recipe-knobs4-7.md    that file). Committing = pre-registration.
#   docs/datasheet.md               — corpus certification + defect registry
#                                     D1-D6 (incl. BROKEN bomb_site label:
#                                     derive site from plant POSITION).
# Old wm_3map*/round-encoder runs = HISTORY (baselines only, never evaluated as
# final). ~15GB superseded artifacts deleted. Phantom numbers purged from docs.
#
# STRUCTURE (2026-07-26 restructure): the inline amendment trails ("ADDED
# 2026-07-26", "R2 ADDITIONS", "R2/PREFLIGHT ADDITIONS") are MERGED into the
# step text below — the amendments are now simply what each step says, with a
# short provenance cite per step (preflight §5 / deep-sweep-r2 §7 / knobs4-7
# §R2-ERRATA / bridge-design §Amendments-R2). The full amendment texts remain
# recoverable via git history + those frozen audit registers.
# docs/execution-checklist.md is the single merged live execution order.
# Dated history lives in docs/progress-history.md (pointer at bottom).
#
# REPO STATE (2026-07-26): VLM-era stack archived (preflight B12 + B7
# consolidation, 2026-07-26): train_grpo.py, train_sft.py,
# build_sft_dataset.py, and the four run_*_pod/smoke/auto_stop wrappers moved
# to scripts/_archive/; the whole src/training/ and src/inference/ packages
# moved to src/_archive/; the duplicate scripts/pod_setup_grpo.sh archived —
# the repo-root pod_setup_grpo.sh is the single canonical pod bootstrap
# (pod-runbook.md + bridge-design.md now both point at it); tests unchanged at
# 23 passed / 1 skipped / 1 xfailed and `grep -rl train_grpo.py scripts/*.sh`
# = 0 live hits.
# History-record caveat: entries in docs/progress-history.md that reference
# scripts/train_grpo.py, the run_*_pod.sh wrappers, src/training/, or
# src/inference/ at live paths (incl. the ARCHIVE header's "Scripts are in
# scripts/_archive/" claim) predate the 2026-07-26 moves; the record was NOT
# edited — those modules now live under scripts/_archive/ and src/_archive/.
#
# --------------------------- ORDERED RUNBOOK (pre-R1) -----------------------
# BLOB GUARDRAIL (reworded 2026-07-26, deep-sweep-r2): val-side blobs only,
# one at a time, torch.load(mmap=True); train blobs (3-10GB) FORBIDDEN in
# agents/sessions. (All val _p1 blobs are 1.77-2.04GB — the old "700MB" figure
# is stale; the val/train distinction is the real rule.)
# [1] PATCH CORPUS (CPU, ~1h, no demos needed). Write scripts/patch_corpus.py:
#     recompute in-place on v2m/v3m blobs: (a) 4 bomb_state bits [carried from
#     has_c4 per-player dim 14; planted from bomb_x/y!=0; site from plant
#     POSITION (xy; z on nuke) NOT the broken metadata label], (b) round_time
#     re-anchored at the in-tensor freeze->live phase transition + its /115 dim
#     + 8 sinusoids, (c) v3 dim7 dist_to_bomb plant-gated (sentinel 1.0).
#     CORRECTED 2026-07-19 (corpus-strategy): full re-bake is FEASIBLE — 224
#     raw .dem (109.7GB) persist on HF skkwowee/chimera-cs2/demos/, 92/92
#     matches covered (split is 70 HF + 22 local). Patch-in-place is right
#     because it is ~100x cheaper (~1h CPU vs 110GB + 20-40 CPU-h re-parse),
#     NOT because there is no alternative.
#     PRECONDITION (HARD): snapshot the merged v2m/v3m blobs + split manifest
#     BEFORE patching — they are the single-copy irreversible asset ([1]
#     mutates them; nothing equivalent exists on HF).
#     REQUIRED: patch lineage stamping ({script sha, transforms, blob sha256
#     pre/post, date}) into blobs + corpus manifest.
#     VALIDATE: re-bake >=1 LOCAL-era AND >=1 HF-era match with the v2.1
#     builder and diff vs the patch (HF-era stems never locally verified);
#     assert every blob meta carries match_id and the set == the 92 manifest
#     keys. This diff doubles as the v2.1 builder's correctness certificate.
#     DONE-CHECK: patched blobs exist + validation diff report in outputs/.
#     >>> [1] DONE 2026-07-19: 4 blobs patched (*_p1.pt); invariants green
#     (suite 23p/1s/1xf); validation PASS (clock PASS-with-caveat; D7
#     tick-dropout disclosure added to datasheet+manifest); defaults
#     flipped to _p1; HF push of patched blobs = last step. NEXT: [2].
#     >>> Correction to [1] (2026-07-26): the earlier "defaults flipped"
#     claim was overstated — at that point only the plan existed; as of
#     2026-07-26 the six eval scripts (rollout_eval, dist_coverage_eval,
#     gen_demo, decision_eval, eval_world_model, value_probe) actually
#     default to the *_p1 blobs (v2m_p1 for eval_world_model/value_probe,
#     v3m_p1 for the rest) and the canonical 5-map set where a --maps flag
#     exists, and all superseded blobs (train/val, *_v3, *_v2m, *_v3m) now
#     live in data/processed/tick_sequences/_superseded/ so stale loads
#     fail loudly.
#     >>> Correction cont. (2026-07-26, repair pass): the remaining blob
#     defaults were flipped to *_p1 the same day — facing_bias_check,
#     event_boundary_check, nla_capacity_probe, gen_bridge_sft (--split;
#     train-side, string edit only), plus inspect_features and vq_killtest
#     (outside B4's nine but caught by the same grep). B4's nine now all
#     default to *_p1 blobs. STILL OPEN from B4: retired/HISTORY --ckpt
#     defaults (outputs/world_model/h8 and outputs/wm_3map*) survive in
#     value_probe, eval_world_model, rollout_eval, decision_eval,
#     dist_coverage_eval, gen_demo, facing_bias_check, event_boundary_check,
#     gen_bridge_sft, nla_capacity_probe — disposition (make --ckpt required
#     vs point at R1 output dirs, and whether world_model/h8 is a retired
#     ckpt or the intended v2m-baseline probe target) awaits owner
#     adjudication; checklist 0.5 is marked PARTIAL accordingly.
# [1b] P2 BLOB PATCH (OWNER DECISION 2026-07-25: run BEFORE [2], not deferred —
#     preflight B6a). corpus-audit §5.B item 3: dropped-bomb bomb_x/y from
#     carrier position at has_c4 falling edge; steamid/name meta enrichment;
#     `place` sidecar for the 81 local stems; (d) crop end-phase frames beyond
#     round_end + 7s (r12 half-boundary rounds carry up to ~60s of dead
#     halftime frames; r24 truncation disclosed, non-boundary rounds pad 7.0s
#     exactly); (e) clamp bomb_age at explosion/defuse (cap 1.0 normalized —
#     currently overflows to 2.27x on r12 rounds). [2]'s quantile fit must not
#     ingest the tail frames. Outputs *_p2.pt + lineage restamp
#     in corpus_manifest.json; [2]-[7] then run on _p2 (defaults updated in the
#     same commit — do not leave _p1 defaults pointing at a superseded corpus).
#     Same validation pattern as [1] (sample diff vs fixed-builder re-bake).
#     DONE-CHECK: _p2 blobs + manifest entry + validation report committed.
#     Provenance: preflight §5 B6a (owner decision 2026-07-25); (d)/(e) =
#     deep-sweep-r2 §7, D8 (2026-07-26).
#     STATE 2026-08-09 (PARTIAL, NOT CANONICAL): P2 writer + fixtures landed;
#     val_v2m_p2 + val_v3m_p2 local artifacts pass post-write legacy/P2
#     invariants and manifest/blob/script hashes. Per-blob tensor report:
#     46 rounds / 7,294 tail frames cropped; 10,507 bomb-age values clamped.
#     Falling-edge drop inference vs 222 verified local bomb events: precision
#     0.982, recall 0.987, median xy error 1.22u. Still required: both train
#     blobs on quiet machine, fresh-builder local/HF diff, then one atomic
#     defaults flip. corpus_manifest.p2_status remains complete=false/canonical=false.
# [2] FIT DIST EDGES (CPU, minutes, QUIET MACHINE — loads 8.7GB train blob).
#     .venv/bin/python scripts/fit_dist_edges.py  on the P2 train_v2m blob,
#     5-map filtered. STATE 2026-08-09: argparse + canonical map default +
#     3,573-round/OOD guards landed and are test-covered; numeric fit still waits
#     for train_v2m_p2 on the quiet machine.
#     Update DIST_EDGES_U in train_world_model.py from output.
#     DONE-CHECK: new edges committed + per-map quantiles in datasheet.
#     Provenance: preflight §5 (B4).
# [3] TRAINER COMPLETION (code, no GPU): DONE 2026-08-09. Detached value head;
#     fixed BCE weight 1.0; one-step sample-and-swap with locked p-ramp and named
#     RNGs; real-frame E1 mask (alive(t) AND alive(t+k) AND NOT freeze(t));
#     dead-player xy decode zeroing; val-ns-only checkpoint selection; snapshots
#     + last.pt; fixture-scoped loud --no-clean; pinned v2/v3 + SS-off commands.
#     test_no_value_leak is green and a forced-p>0 SS path is test-covered.
#     Gradient-based on purpose: detachment makes the trunk
#     gradient-identical to value_weight=0, so keeping the head + proving it
#     with the gradient rail beats banning the head.
#     STATE: CPU suite 40 passed / 1 skipped, ruff green. evaluate() CUDA device
#     bug remains fixed (won indexed via CPU mask — R2-B1, fixed 2026-07-26).
#     Provenance: preflight §5; R2-B1 fix + --no-clean = deep-sweep-r2 §7
#     (2026-07-26); mask errata = knobs4-7 §R2-ERRATA.
# [3b] CUDA SMOKE GATE (NEW, deep-sweep-r2): 30 steps on the 4090 incl. at
#     least one evaluate() pass, BEFORE any pilot/pod run. CI is CPU-only and
#     structurally cannot see device bugs; R2-B1 died exactly at first eval.
#     DONE 2026-08-09: local RTX 4090, dist head, 30/30 steps, evals at 15/30,
#     finite val_ns 0.0200 -> 0.0124; best_ns.pt + last.pt, no retired best.pt.
#     Tiny-smoke metrics validate the device path only, never model quality.
#     Provenance: deep-sweep-r2 §7.
# [4] COVERAGE HARNESS (code): extend rollout_eval.py to K sampled rollouts
#     (minADE-K, frozen K/temperature) — doubles as the GRPO group generator.
#     CHANGE B (first-principles-plan): MUST ship with (i) the fair stochastic
#     baseline (per-bucket damped-CV + fitted residual covariance, K=16, scored
#     identically) — coverage numbers are UNREPORTABLE without it; (ii) a
#     trajectory-coherence metric (mode-switch rate of depth-10/20 rollouts vs
#     matched real trajectories) — the only instrument that sees mode-dithering;
#     (iii) the 64Hz-truth column (corpus-strategy §2 row 13, pre-registered —
#     do not build the harness without it); (iv) cheap joint-coherence
#     interaction control (inference-only): decode with cross-player attention
#     masked (or per-player marginal sampler) on coverage + coherence; if gap
#     ~ 0, soften "10 coupled agents" wording BEFORE review.
#     DONE-CHECK: sampled-coverage smoke on val runs incl. baseline AND 64Hz
#     columns (B6, wording per preflight §5 4.1).
#     Provenance: (i)/(ii) = first-principles-plan CHANGE B; (iii)/(iv) =
#     preflight §5 + deep-sweep-r2 §7.
# [5] LOCAL SMOKE of the full canonical config (v2, seed 0, small steps).
# [pre-6] PRE-REGISTRATION SITTING (doc-level, MUST precede any [6] launch —
#     unfixable honestly after results exist): knobs4-7 §R2-ERRATA holds the
#     amendments (C1 cluster-CI conjunction, C1-REP statistic pinning, v2->v3
#     promotion rule, SS-vs-TF statistic + branch, OOD zeroed-ID criterion,
#     OOD probe-transfer scope). Remaining tasks: power/MDE simulation (CPU
#     afternoon; record MDE per gate; +2-seed escalation pre-committed if
#     C1-SCALE MDE > 0.01); commit the probe-select split manifest; grounded-
#     GRPO claim schema one-pager (R2-B4, per amendment F's own sequencing).
# [6] RUNS (pod, ~$45-85, ONLY on user go): 3 seeds x {v2,v3} + SS-off control
#     = 7 runs; + co-trained twin (weight 0.3, no detach), 1 run, secondary row
#     only (preflight B10, added 2026-07-26);
#     + round-encoder retrain on clean corpus (Knob 7, v6 config);
#     + keystone WM data-scaling curve (reuses the 3 canonical v2 runs);
#     + CHANGE C: matched-capacity SUPERVISED ceiling — same 19M arch, clean
#     corpus, end-to-end on round_won (+event heads), probed under the identical
#     protocol (hours on 4090, no pod). Kills the legacy-strawman objection.
# [7] GATES: keystone probes (pre-registered criteria in knobs4-7 doc §Knob 7),
#     OOD overpass protocol (Knob 4), THEN bridge/GRPO ladder (L3-L5) with:
#     CHANGE D (pre-QLoRA referees, CPU/4090, BEFORE any 35B pod spend):
#       (i) grid-target capacity probe ([11,512] recon from head-output text vs
#           latent) — decides soft-tokens vs head-readout; (ii) SAE/linear-probe
#       audit for latent features not decodable from head outputs; (iii) bridge
#       tap layer sweep (layers 3-6, steerability + persistence — J-space-
#       inspired); promote head-Jacobian subspace to PRIMARY recon target.
#     CHANGE E (gate work): B2 implemented as exact-match fact audit vs tick
#       ground truth; ADD counterfactual flip-test (edit man-advantage/bomb
#       state, re-encode through frozen WM, demand the text flips) as a
#       co-equal causal gate leg.
#     CHANGE F (before on-policy GRPO): checker feasibility gate (text->claim
#       extractor AUC >= ~0.75 on pseudo-gold; CRPS/Brier scoring, not binary);
#       reward-noise ICC gate (>= ~0.2 under single-draw future noise, else
#       advantages are noise); one offline ReST/rejection-sampling round with
#       the validated checker FIRST. Reward is GROUNDED-vs-actual-future
#       (CHANGE A; bridge-design §5) — value-through-rollout = generator only.
#     BRIDGE AMENDMENTS (all pre-registered BEFORE bridge spend):
#     state-as-text baseline arm (A-R2.1, co-equal, non-negotiable);
#     head-Jacobian target as math + nla_capacity_probe emits projection
#     matrix (A-R2.2); group semantics DECIDED G=16 completions (A-R2.3);
#     readability leg protocol (A-R2.4); CHANGE-E leg 5 fact-flip/paraphrase
#     probes (A-R2.5); NEW step between CHANGE F and on-policy GRPO: build +
#     CPU-smoke the manual-loop trainer, measured sec/step -> $ budget before
#     go (A-R2.6). CHANGE-F PRECONDITION: val kills side-files +
#     event_boundary_check hard-fail + AUC recompute (pipeline Tier-B #11)
#     must be done or its gates cannot honestly run.
#     OPEN OWNER DECISIONS (decisions-ledger 2026-07-26): events-reparse
#     scope (all 92 vs restricted prompt set); MLMove learned-baseline column.
#     Provenance: preflight §5; first-principles-plan CHANGEs A/D/E/F;
#     A-R2.1–A-R2.6 = bridge-design §Amendments-R2 (2026-07-26).
# ----------------------------------------------------------------------------
# Machine notes: WSL 22GB + 8GB swap (.wslconfig LIVE, verified free -h 21Gi; dated correction 2026-07-26 — the 15GB figure was pre-shutdown); GPU = display GPU; workflow crashes = CLI-process
# exits (NOT memory — see ~/.claude/skills/workflow-recover/SKILL.md; start
# sessions with `claude 2>>~/.claude/cli-stderr.log` to capture the next one).
#
# ============================================================================
# Dated history (2026-06-09 verdicts, 2026-06-14 NLA notes, the 2026-06 pivot block, and the VLM-era ARCHIVE with 2026-03 → 2026-05 sessions) moved VERBATIM to docs/progress-history.md on 2026-07-26 — it is a RECORD; read it there, never edit it.
# ============================================================================
